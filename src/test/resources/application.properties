# Default test configuration - allows H2 in-memory database for legacy tests
# BaseIntegrationTest subclasses should use @TestProfile(PostgreSQLTestProfile.class) 
# to override with PostgreSQL Testcontainers

# Disable database version check to allow H2 emulation mode
quarkus.hibernate-orm.database.version-check.enabled=false
